// TODO: complete per Expo Notifications guide if you need push.
// This file holds placeholders for later wiring so the imports don't break.

export async function registerForPush() {
  // Follow: https://docs.expo.dev/push-notifications/overview/
  // 1) Install expo-notifications
  // 2) Request permissions
  // 3) Get Expo push token and send to your backend if needed
  return null
}
