# eToolkit

## Mobile Payments

Install the Stripe React Native SDK:

```bash
expo install @stripe/stripe-react-native
```

Ensure iOS apps have the proper `merchantIdentifier` entitlement and Android has Google Play billing set up.

Use Stripe test cards such as `4242 4242 4242 4242` for testing. Apple Pay and Google Pay setup will be added later.

